name: Upgrade Tools

on:
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  upgrade:
    uses: ./.github/workflows/upgrade-tools.yaml
